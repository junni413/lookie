<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="lookie.backend.domain.location.mapper.LocationMapper">

    <select id="findByCode" resultType="LocationVO">
        SELECT
            location_id AS locationId,
            zone_id AS zoneId,
            location_code AS locationCode
        FROM zone_locations
        WHERE location_code = #{locationCode}
          AND is_active = TRUE
    </select>

</mapper>
