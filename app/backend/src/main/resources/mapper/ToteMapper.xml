<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="lookie.backend.domain.tote.mapper.ToteMapper">

    <select id="findByBarcode" resultType="ToteVO">
        SELECT
            tote_id AS toteId,
            current_batch_task_id AS currentBatchTaskId,
            barcode,
            is_active AS isActive
        FROM totes
        WHERE barcode = #{barcode}
    </select>

    <update id="updateToteMapping">
        UPDATE totes
        SET current_batch_task_id = #{batchTaskId}
        WHERE tote_id = #{toteId}
    </update>

</mapper>
