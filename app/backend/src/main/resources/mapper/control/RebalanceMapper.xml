<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="lookie.backend.domain.control.mapper.RebalanceMapper">

    <!-- 
        특정 배치의 최신 스냅샷 조회
        1. 해당 batch_id의 MAX(ts)를 서브쿼리로 구함
        2. 그 ts와 batch_id가 일치하는 모든 row 조회
    -->
    <select id="selectLatestSnapshots" resultType="lookie.backend.domain.control.vo.RebalanceSnapshotVO">
        SELECT
            ts,
            batch_id AS batchId,
            worker_id AS workerId,
            zone_id AS zoneId,
            progress,
            remaining_qty AS remainingQty,
            time_to_planned_end_min AS timeToPlannedEndMin,
            time_to_deadline_min AS timeToDeadlineMin,
            zone_backlog AS zoneBacklog,
            zone_active_workers AS zoneActiveWorkers,
            zone_location_cnt AS zoneLocationCnt,
            zone_blocking_issue_cnt AS zoneBlockingIssueCnt,
            worker_speed_30m_avg AS workerSpeed30mAvg,
            speed_label AS speedLabel,
            picked_total AS pickedTotal,
            required_total AS requiredTotal,
            created_at AS createdAt
        FROM rebalance_snapshots
        WHERE batch_id = #{batchId}
          AND ts = (
              SELECT MAX(ts)
              FROM rebalance_snapshots
              WHERE batch_id = #{batchId}
          )
    </select>

</mapper>
